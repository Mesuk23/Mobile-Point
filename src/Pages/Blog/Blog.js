import React from 'react';
import useTitle from '../../Hooks/Usetitle';

const Blog = () => {
    useTitle('Blog');
    return (
        <div>
            blog
        </div>
    );
};

export default Blog;